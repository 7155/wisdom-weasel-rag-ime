# 原始五类指标完成账本

本账本保留用户原始五类范围，区分“指标已测量”和“收益目标已获证”。早期显式 handoff 与 CLI 不能替代真实 Memory consumer 或可操作 App；后续已分别补测。完整结果见 [FIVE_METRIC_RESULTS.md](FIVE_METRIC_RESULTS.md)。

用户目标：**完成这些指标**；控制为“小任务、token 有限、简历数据、Lab/Trace 自举”。本次完成有边界的小样本测量与可复验交付，不要求所有对照都出现收益，也不将结果扩大成生产或通用能力声明。

新增要求“优化完成。测出收益，才有故事”已取得 [单独工作流收益](OPTIMIZATION_RESULTS.md)：Room 模型整合→程序校验合并，同输入三个检查保持 3/3，确认调用 3→2、tokens -35.5%、估算成本 -24.7%。该结果补充 M1/M4 的编排与自举证据；不替换旧单 Agent 对照，不改变下表的历史分母。本轮八次真实调用含诊断、首轮超预算结算和确认，合计 26,801 tokens、估算 $0.00577524。

| ID | 原始成果与指标 | 已验收的实际结果 | 未证明的更广结论 |
| --- | --- | --- | --- |
| M1 | Room 分工与交接；同任务同预算完成率/耗时/成本、交接遗漏、重复工作 | 同模型同预算上限三 case：两侧均 2/3；成本/时间完整对账；正确交接遗漏 0/2、分配重叠与重复发现行 0 | 没有协作增益；串行脚本分工不证明自主拆解或并行加速 |
| M2 | 上下文能接续；接续成功、关键事实召回、错误/过期使用、上下文用量 | 真 Memory consumer → 四个真实 Pi Session：答案 4/4；正例召回 2/2、负例排除 2/2；未观察错误/过期使用；完整 turn 10,746 tokens | 初始 ACK/compaction summary 是 fixture；生产长期记忆和自动压缩收益未测 |
| M3 | 发送、停止、重连后状态正确；故障通过、重复执行、取消收口、恢复结果 | 8 类 × 20 次 = 160/160；重复副作用 0；取消与恢复均有逐观察计时及分组 p95 | 真实 Lab owner/SQLite、模拟业务、进程内 owner 重建；不宣称网络断包或 native 验收 |
| M4 | 企业配置优化保质降本；任务通过、严重错误、每成功任务成本、耗时、优化投入 | 四历史场景前后质量/成功单位/成本投影；另以小费用闭环完整记录两轮 7 次调用、17,715 tokens、$0.004189，禁止批准 0/3，候选无成本收益 | 历史完整优化/工程/Judge 成本及历史业务严重性仍未知；不填 0、不声称盲测 |
| M5 | 优化配置能交付独立使用；导出/干净启动/前后一致 | 同一 Lab 配置导出独立费用 Web App：导出 1/1、隔离启动 3/3、4 条业务 × 3 次 12/12、真实 UI 4/4、前后一致 | 掌柜问数静态包仍需 Gateway/Pi；不是原生包或另一台实体机器验收 |

## 协作与写入责任

主 Session：M1/M3 与共同验收、所有共享文档。记忆子任务仅写自己的 continuation runner/test；App 子任务仅写 export acceptance runner/test；企业子任务仅写 optimization summarizer/test。三个子任务不自行调用付费 Provider、不安装、不提交、不改共享账本。

三个 lane 已由主 Session 复核；修正了负例当 Recall、执行信号当业务严重错误、CA 当完整任务成功、跳过 UI 却标通过等口径。主 Session 另完成真实 Memory 回答、完整 turn 复算与独立费用 Web App 的实际操作验收。

## 数据规模与因果边界

每个评测用例保持小输入、固定分母。Room 新比较的三 case 来自本次已实际遇到的 PAW receipt identity、quality/state、whole-turn usage 契约；不是公共榜单，也不是大规模复杂任务收益。每臂冻结相同 `4 Provider calls / 12k observed tokens / 768 output tokens per call`，按真实 transcript 对账，预算超额单列。

可靠性重复运行主要不调用模型。实际时延应标明源码 owner、本机 SQLite 和模拟业务效果；不自动推导线上 P95 或 native 重连成功。

五类指标均已有可复验的小样本或明确负结果。更广收益与历史缺失字段独立保留；不要求重跑大型基准或继续烧 token 来追求全绿。

2026-09-06 补充：中等 JSONL 账本任务已测至每臂累计 300k 观察 tokens / 30 条模型响应上限；两侧完整任务均 0/1、实现检查 16/20、交付 0/4。Room 进入真实重启后的接续阶段，仍未完成整题；已知费用与未知在途成本分开。此结果不扩大上述 Room 正向结论，详见 [中等任务](MEDIUM_TASK_RESULTS.md)。八条当前结果已进入 [OS Lab 图表](OS_RESULTS.md)，原失败与旧版本保留。

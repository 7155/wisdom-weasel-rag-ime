RAG-IME DB cleanup handoff - 2026-07-06

Repo:
  /Volumes/undo 4t/git/learnA/wisdom-weasel-rag-ime

Branch observed:
  codex/wisdom-weasel-rag-ime-mvp

Primary user intent:
  Finish organizing the current RAG database for the input-method project.
  The raw input records are messy. The user wants x1api / 5.4-mini used for
  offline RAG cleanup, but x1api must not be called concurrently.

Important runtime boundary:
  /rime-suggest realtime path must stay local/loopback.
  Remote/high-intelligence models such as x1api 5.4-mini are only for offline
  cleanup, memory compilation, RAG database organization, lexicon/tag/alias
  generation, and reviewable drafts.

User correction:
  x1api cannot be called concurrently.
  5.4-mini supports KV cache. For future batch cleanup, use stable prompts and
  sequential chunked requests so provider-side cache can help without parallel
  pressure. In this run, memory-compile was a single request, so no parallel
  x1api calls were made.

Database:
  /Users/undo/Library/Application Support/RagIme/rag-ime.sqlite

Backup made before apply:
  /Users/undo/Library/Application Support/RagIme/backups/rag-ime-before-organize-20260706-165210.sqlite

Initial database shape observed:
  input_events: 10137
  memory_items: 15
  memory_atoms: 0
  memory_aliases: 0
  memory_tags: 80
  memory_feedback_events: 674

Main source distribution before cleanup:
  codex_history / codex-history-import / wisdom-weasel-rag-ime: 9896
  squirrel_rime_sidecar / rime-sidecar:model: 113
  squirrel_rime_sidecar / rime-sidecar:rag: 88
  curated_user_feedback / ime-adapter: 11
  manual_commit / ime-adapter: 7
  several x1api/gpt-5.5 generated optimizer rows also existed.

Interpretation:
  The user's diagnosis is correct. The database has many raw/imported rows,
  especially Codex history, but only a thin curated memory layer. The high-value
  work is not to stuff more raw transcripts into retrieval. It is to compile
  stable memory_items, future memory_atoms, aliases, tags, and short surface
  hints from raw events and feedback.

Applied deterministic cleanup:
  Command:
    python3 -m rag_ime.cli --db-path "$HOME/Library/Application Support/RagIme/rag-ime.sqlite" organize-rag-db --project wisdom-weasel-rag-ime --sample-size 0

  Dry-run before apply:
    scanned: 4011
    matchedNoise/wouldHide: 723
    duplicate_low_value_text: 27
    generated_side_candidate_oneoff: 10
    ime_complaint_or_debug_feedback: 1
    similar_low_value_text: 685

  Apply result:
    hidden: 723
    removedHiddenVectors: 723

Applied local-rule cleanup diff:
  Plan:
    /tmp/rag-ime-cleanup-local-rule-20260706-165333.json

  Validate:
    ok: true
    diffCount: 7
    counts:
      addStableMemory: 7
      addPhrase: 0
      downrank: 0
      tombstone: 0

  Apply:
    ok: true
    runId: cleanup_1783328013744
    status: applied

Database shape after cleanup:
  input_events_total: 10137
  active_events: 3288
  deleted_events: 6849
  memory_items: 22
  memory_atoms: 0
  memory_aliases: 0
  memory_cleanup_runs: 1
  memory_cleanup_diffs: 7

x1api / 5.4-mini attempts:
  Attempt 1:
    Command used memory-compile with:
      --provider x1top
      --model gpt-5.4-mini
      --model-env-path ~/.rag-ime-x1api.env
      --save-draft
    Result:
      HTTP 502: error code: 502

  Non-secret env values observed:
    X1API_BASE_URL: x1api.top
    X1API_MODEL: gpt-5.5
    RAG_IME_AI_WIRE_API: responses
    RAG_IME_AI_TIMEOUT_SECONDS: 60
    RAG_IME_AI_REASONING_EFFORT: xhigh

  Attempt 2:
    Created a temporary env file with:
      X1API_MODEL=gpt-5.4-mini
      RAG_IME_AI_REASONING_EFFORT=low
    Single sequential request, temp env deleted afterwards.
    Result:
      HTTP 502: error code: 502

  Conclusion:
    The 5.4-mini x1api cleanup call did not succeed in this run. This was not a
    concurrency issue from this agent because calls were sequential. Likely next
    checks are x1api model availability, gateway health, expected model id, and
    whether the provider expects a different wire API or model alias.

Code change made:
  File:
    rag_ime/local_sqlite_core.py

  Reason:
    organize-rag-db dry-run on the real database was too slow / effectively
    stuck with about 10k imported rows. The similar-text duplicate detector did
    fuzzy SequenceMatcher comparisons across too many candidate pairs.

  Patch behavior:
    _rag_database_similar_duplicate_plan now builds deterministic blocking keys
    for each normalized text before running expensive fuzzy comparison. It uses
    length bucket, prefix, suffix, middle window, and bounded n-gram windows.
    This keeps the old semantic rule for deciding similarity, but shrinks the
    candidate pool so real databases finish.

  New helper:
    _rag_similarity_block_keys(text: str) -> tuple[str, ...]

Continuation in current Codex run:
  Implemented Prediction Stability and Visible Diagnostics follow-up from the
  pasted txt:
    1. PredictionManager source cache now keys on queryAnchor instead of the
       committed-context tail. Same committed context but different raw/preedit
       or Rime top candidates no longer reuses old LLM/RAG source cache.
       StablePanelState is responsible for soft-hold/prefix-filter display.
    2. Debug server now records a redacted Live Prediction Trace for recent
       /rime-suggest frames and exposes:
         GET /api/prediction/live-trace?limit=100
         GET /api/prediction/drop-stats?limit=500
       The trace includes anchors, lane status, drop reasons, source counts,
       status-row count, and candidate metadata. Raw text is hidden by default.
    3. Product readiness gate now separates frontend runtime DB from deterministic
       quality DB. In macOS mode, --db-path remains the installed Squirrel/RAG-IME
       runtime DB for config/readiness checks, while --quality-db-path points at
       the seeded gate DB for repeatable eval cases.

  Validation in current run:
    python3 -m unittest tests.test_prediction_manager tests.test_prediction_anchors \
      tests.test_prediction_stability tests.test_prediction_status tests.test_side_lane_scheduler
    Result: OK

    python3 -m unittest tests.test_debug_server tests.test_rime_sidecar tests.test_prediction_manager
    Result: OK

    python3 -m unittest tests.test_product_readiness_gate tests.test_codex_history
    Result: OK

    python3 -m unittest tests.test_local_sqlite_core tests.test_cleanup_pipeline
    Result: OK

    python3 -W error::ResourceWarning -m unittest discover -s tests
    Result: Ran 652 tests in 131.049s, OK.

  Product-gate status in current run:
    Installed RAG-IME input source is selected and latest:
      im.rag-ime.inputmethod.RagIme.Hans

    Backend deterministic quality gate passed with seeded gate DB:
      rag pass-rate: 0.9706
      rime-sidecar pass-rate: 0.9706
      old-input echo rate: 0.0

    macOS tryout quality gate also passed after adding --quality-db-path, but
    the overall tryout gate still fails because LaunchServices still reports a
    duplicate root-owned disabled app:
      /Users/undo/Desktop/rag-ime-input-method-cleanup-20260703-205424/system/RAG-IME.app.disabled

    Codex cannot move it non-interactively because sudo requires a password.
    Manual cleanup command from the gate:
      sudo mkdir -p '/Users/undo/Library/Application Support/RagIme/disabled-input-method-backups'
      sudo mv /Users/undo/Desktop/rag-ime-input-method-cleanup-20260703-205424/system/RAG-IME.app.disabled \
        '/Users/undo/Library/Application Support/RagIme/disabled-input-method-backups/RAG-IME.app.disabled.disabled-bundle-'$(date +%Y%m%d-%H%M%S)
      RAG_IME_QUARANTINE_STALE_SQUIRREL_APPS=1 scripts/refresh_squirrel_input_source_registration.sh
      python3 scripts/audit_squirrel_input_source.py --input-source-id im.rag-ime.inputmethod.RagIme.Hans --bundle-id im.rag-ime.inputmethod.RagIme

Validation commands run:
  python3 -m unittest \
    tests.test_local_sqlite_core.LocalSqliteCoreClientTests.test_organize_rag_database_hides_generated_oneoffs_and_complaints \
    tests.test_local_sqlite_core.LocalSqliteCoreClientTests.test_organize_rag_database_reports_and_hides_low_value_duplicate_text \
    tests.test_local_sqlite_core.LocalSqliteCoreClientTests.test_organize_rag_database_reports_and_hides_similar_old_input_text \
    tests.test_cleanup_pipeline.CleanupPipelineCliTests.test_cleanup_preview_local_rule_is_dry_run_and_apply_requires_flag

  Result:
    Ran 4 tests in 0.293s
    OK

Current data-source judgment for high-quality RAG:
  Best sources:
    1. Accepted IME candidates and user-pinned/downranked feedback.
    2. Repeated user-authored phrases with project/app context.
    3. Stable project requirements and user preferences from Codex chats.
    4. Codex user messages distilled into preferences, project facts, commands,
       and Q/A memories.
    5. Agent answers only as offline evidence for distillation, not raw realtime
       candidates.
    6. Manual aliases, technical terms, short surface hints, and tag graph edges.

  Lower-quality sources:
    1. Raw Codex transcripts imported directly as candidate text.
    2. One-off generated model/RAG side candidates without accepts.
    3. Debug complaints and temporary runtime status messages.
    4. Assistant boilerplate and tool output.
    5. Long historical sentences displayed directly in the IME candidate panel.

Recommended next plan:
  1. Fix or confirm x1api 5.4-mini access using a tiny non-sensitive one-shot
     request before running memory-compile again.
  2. Add a sequential batch memory compiler that chunks Codex history by day /
     project / topic and uses stable prompts to benefit from KV cache.
  3. Add Memory Book v1 only after the current cleanup pipeline is stable:
       memory_books
       memory_book_fts
       daily/project/topic book compiler
  4. Keep Codex history as evidence source. Do not allow raw transcript rows to
     dominate realtime retrieval.
  5. Implement atoms/aliases next:
       memory_atoms for stable facts/preferences/tasks
       memory_aliases for surface_hint/query_expansion/pinyin/abbrev
  6. Re-run eval-memory-optimizer and realistic /rime-suggest probes after the
     Memory Book/alias layer is populated.

Open risks:
  1. x1api 5.4-mini returned 502 twice, including low reasoning effort.
  2. memory_atoms and memory_aliases remain empty, so v3 retrieval is not yet
     getting the intended tag/alias/atom benefits.
  3. A root-owned duplicate disabled RAG-IME app still needs manual sudo cleanup
     before the macOS product gate can be fully clean.
  4. Foreground soak / real typing trace still requires user-operated typing
     after duplicate registration cleanup.

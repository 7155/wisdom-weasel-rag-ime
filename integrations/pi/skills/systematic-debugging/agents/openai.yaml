interface:
  display_name: "Debug Systematically"
  short_description: "Reproduce and localize unknown failures before fixing"
  default_prompt: "Use $systematic-debugging to reproduce and localize the failure, verify separately that the real path runs and satisfies the precise requirement, and return any failed acceptance to its responsible stage."

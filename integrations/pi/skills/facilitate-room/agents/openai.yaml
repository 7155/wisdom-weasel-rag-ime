interface:
  display_name: "Facilitate Room"
  short_description: "Coordinate visible responsibilities and one Root result"
  default_prompt: "Use $facilitate-room only as the designated Room Facilitator when the current request truly needs multiple visible responsibilities, dependency stages, or useful concurrency. Choose the smallest topology, integrate Partner submissions as evidence rather than acceptance, verify operability and requirement satisfaction separately, reconcile every WorkItem, and publish one truthful Root result."

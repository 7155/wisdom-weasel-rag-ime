interface:
  display_name: "Align and Decide"
  short_description: "Inspect facts and settle material user-owned choices"
  default_prompt: "Use $alignment-and-decision to inspect available facts and settle only the material choices that belong to the user."

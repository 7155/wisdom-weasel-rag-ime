interface:
  display_name: "Align and Decide"
  short_description: "Preserve intent, clarify the work, and grill decisions when requested"
  default_prompt: "Use $alignment-and-decision to preserve the original request and vision, investigate uncertainty, sharpen domain language, ask one useful question at a time, and use full Grill Mode when requested."

interface:
  display_name: "Plan Implementation"
  short_description: "Plan verifiable multi-step work without executing it"
  default_prompt: "Use $implementation-planning to turn confirmed acceptance into the smallest dependency-aware WorkItems with explicit responsibility and separate operability and requirement-satisfaction checks."

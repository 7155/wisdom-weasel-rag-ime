export default function registerVerticalAgentSandbox(_pi: unknown) {
  // Enabling this content-addressed Pi Package is the capability grant. PAW's
  // per-Session runtime manifest then projects the Host-owned `sandbox` Tool.
  // Keeping credentials and Session identity out of package code is required
  // by the multi-Session Runtime Host boundary.
}

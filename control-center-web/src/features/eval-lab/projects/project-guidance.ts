export type ProjectGuidanceMode = 'auto' | 'guided' | 'sample';

/** One user request to the existing Pi Session, never a frontend execution loop. */
export function projectGuidanceMessage(mode: ProjectGuidanceMode): string {
  const approach = mode === 'auto'
    ? '请自主推进这个 Lab 项目。在已有授权和预算内连续完成能检查、修改和验证的工作；只在需要我补充不可获得的材料、业务判断或新增费用授权时询问，完成这些独立准备工作后再停下。'
    : mode === 'sample'
      ? '请使用明确标注的示例材料，带我走通当前 Lab 项目的最小完整流程。优先读取项目内可用的示例；没有时创建与场景相关的小型合成材料和评测集，并单独标记其来源。示例结果不能作为真实业务效果、历史复跑或生产验证。'
      : '请带我逐步完成这个 Lab 项目。先完成当前可执行的一步，展示结果和下一步；只有存在影响业务结果的选择时，才用一个简短问题和具体选项带我决定。不要反复询问已经确认的方向。';
  return `${approach}\n\n先用 lab_project 读取当前项目、材料、成果和执行绑定，按需使用 agent-lab-project 与领域 Skill。根据真实状态恢复已有工作，不要重新开始或重复运行。先检查项目执行目录和已有公开材料；缺少资料时给出上传文件、粘贴正文、输入路径或使用示例的具体入口，并继续可独立完成的准备。\n\n把计划与过程维护为同一个可视化成果，使用 paw.lab-project-progress.v1 或适合本项目的交互页面；记录步骤、依赖、当前阻塞、已保存结果及下一步动作。将真实材料和案例连接到方案、改动、指标与最终应用。不要用大段 JSON 或状态文字代替可阅读、可操作的结果。\n\n优化时先保存基线与通过标准，每轮改一个主要因素，在预算内运行对应执行器，检查原始回执与失败案例，再决定保留候选、沿用基线或继续下一轮。已存在的运行先 execution_read 核对，不重复提交未知结果的调用。材料、评测、配置和运行版本保持关联。没有完成回执就显示待执行或阻塞，不能生成假指标。候选感知评测、示例、开发集和保留集分别说明。只在已有预算内调用模型，不无限迭代。\n\n可交付时整理选择理由与适用范围，准备可试用、可导出的应用版本。应用应展示真实过程、结果结构和来源；实际发布、外部服务和模型调用继续遵循现有执行边界。`;
}

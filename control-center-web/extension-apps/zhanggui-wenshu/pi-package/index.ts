export default function registerZhangguiWenshuPackage(_pi: unknown) {
  // The package grants the App-specific Skill and identity capability. The
  // ordinary PAW Session remains the only Agent and Tool-loop owner.
}
